import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0C0C0C); // Głęboka czerń
  static const Color pureBlack = Color(0xFF000000);
  static const Color accentRed = Color(0xFFD32F2F); // Krwista czerwień
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textSilver = Color(0xFFB0B0B0); // Jasnoszary/Silver
  static const Color cardBackground = Color(0xFF1A1A1A); // Ciemnoszary dla kart
}

class AppPadding {
  static const double small = 8.0;
  static const double medium = 16.0;
  static const double large = 24.0;
  static const double extraLarge = 32.0;
}

class AppRadius {
  static const double medium = 12.0;
  static const double large = 20.0;
}
