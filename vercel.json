{
  "buildCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Dependencies and build handled by buildCommand'"
}
